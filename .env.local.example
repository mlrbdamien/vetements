VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Aucun identifiant de compte ici : le poste se relie une fois, à la main,
# depuis l'écran de mise en service. Une variable VITE_* est compilée en clair
# dans le JavaScript servi — un mot de passe n'y a pas sa place.
